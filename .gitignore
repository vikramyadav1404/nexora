node_modules/
.env
.env.local
.env.*.local
server/.env
client/.env
client/.env.production
client/dist/
dist/
uploads/
server/uploads/
*.log
.DS_Store
*.pem
node_modules/
server/node_modules/
client/node_modules/
.vercel

# local-only DB connection string (never commit)
server/.db-url.txt
server/.env.backup

# database backups — contain real email addresses
server/backups/

# Supabase personal access token — never commit
server/.supabase-token.txt

# Generated by "npm run migration:runner" — build it, paste it, do not commit it.
# The numbered migrations in server/db/migrations/ are the source of truth.
server/db/migrations/_runner.generated.sql
# Written by npm run stamp at deploy time; never committed.
server/version.generated.json
