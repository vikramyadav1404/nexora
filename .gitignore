node_modules/
.env
.env.local
.env.*.local
server/.env
client/.env
client/.env.production
client/dist/
dist/
uploads/
server/uploads/
*.log
.DS_Store
*.pem
node_modules/
server/node_modules/
client/node_modules/
.vercel

# local-only DB connection string (never commit)
server/.db-url.txt
server/.env.backup

# database backups — contain real email addresses
server/backups/

# Supabase personal access token — never commit
server/.supabase-token.txt
